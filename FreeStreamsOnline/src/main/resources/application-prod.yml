# Available levels: TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
logging:
    level:
        ru:
            ulstu:
                is:
                    server: INFO
spring:
    datasource:
        url: jdbc:postgresql://127.0.0.1/fs_online
        username: postgres
        password: postgres
        driver-class-name: org.postgresql.Driver
    jpa:
        show-sql: false
